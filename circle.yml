machine:
  timezone: Asia/Tokyo

dependencies:
  pre:
    - sudo apt-get install lftp
    - npm install
test:
  pre:
    - echo "PRE:テスト準備"
  override:
    - echo "テストコマンド上書き"
  post:
    - echo "POST:ビルド後処理"

deployment:
  deploy:
      branch: develop
      commands:
        - npm run build
        - ./ftp.sh
